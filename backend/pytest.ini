[pytest]
markers =
    system: System-level tests that run the full application process.
    api: API endpoint tests.
    integration: Integration tests between multiple components.
    service: Unit tests for specific backend services.
